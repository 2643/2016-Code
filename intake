public void teleopPeriodic() {
    Talon intakeMotor = new Talon(6);
    Joystick gamePad = new Joystick(0);
    
    int speed = 0;
    
    if(gamePad.getRawButton(0)){
    	intakeMotor.set(speed);
    }
    else if(gamePad.getRawButton(1)){
    	intakeMotor.set(-speed);
    }
    }
